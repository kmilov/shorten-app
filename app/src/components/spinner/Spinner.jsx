import React from 'react'
import styles from './_Spinner.css'

const Spinner = props => <div className={styles.spinnerContainer}>
  <div className={styles.spinner}></div>
</div>

export default Spinner
